
#include "depth_relative.h"

using namespace wn::distance;
using namespace std;


depth_relative::depth_relative(const wn::hyperonym_graph& graph) : graph(graph) {
}

depth_relative::~depth_relative() {
}

